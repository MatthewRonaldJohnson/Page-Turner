<div class="container my-5">
    <div class="row h-25">
        <div class="col-3 d-flex flex-column">
            <img class="flex-grow-0 flex-shrink-0" src="{{cover_img_url}}" alt="book">
            {{#if rating}}
            <span class="badge bg-success">👍 A Real Page Turner</span>
            {{else}}
            <span class="badge bg-danger">👎 Give it a Pass</span>
            {{/if}}
        </div>
        <div class="d-flex flex-column justify-content-center align-items-center col-9">
            <h4>{{title}}</h4>
            <p>{{body}}</p>
            <p>- {{userName}}</p>
        </div>
    </div>
    {{#each commentArr}}
        {{> comments}}
    {{/each}}
</div>